/* Approved fixed clips belong here after listening/budget approval.
   Deliberately empty: no synthetic fixture is shipped as Pip's voice. */
window.PipVoiceManifest={scriptVersion:'story-v2',styleVersion:'pip-v1',clips:[]};

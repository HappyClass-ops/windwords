# Independent games, shared guidance

Pip’s English is a desktop umbrella and a small handbook repository, not a shared
runtime engine. Games keep separate repositories/releases because they serve
different classroom uses. Windwords keeps its current Pages URL. Reuse assets by
versioned catalogue and copying approved files, not by introducing a package system
before another game actually consumes it.


# Organized workflow and reported-fixes release

Main code commit: e8a60ca. Pages built it and served meanings.js,
run-supplies.js and voice.js with HTTP 200. Final deployment SHA is recorded on #8.
Follow-up: fixed runtime heart capacity/display at three while retaining the old
stored maxHearts purchase; tests/world.cjs passed again with a legacy four-heart
save and an assertion for exactly three displayed hearts.

Code gate: npm test passed (all nine test programs); git diff --check passed.
Headless Chrome checked desktop and 390px layouts. Screenshots remain local in
tests/; generated previews are ignored. No new image/sound generation this batch.

| Ticket | Evidence |
| --- | --- |
| #2 | Painted sign labels centered, nowrap, readable; desktop/phone screenshot inspection |
| #3 | Soft multi-lobed cloud mask; locked names remain ????; browser computed-style check and screenshot |
| #4 | Exact-POS dictionary adapter, six original homograph overrides; mocked cache/missing/offline tests and spoken-meaning/stale-close browser assertions |
| #5 | Failing completion-contract test reproduced missing Promise; fake audio end/cancel and banner caller now pass; fixed timer no longer cuts the banner off |
| #6 | First-movement direction check; direct open-floor route; items smaller and placed on counter; screenshots inspected |
| #7 | Run-only wallet, caps, insufficient funds, double-click rejection, resets and Learning isolation; actual beacon → shop → purchase → route flow; skip resolves once; legacy stored purchases preserved but not applied |
| #8 | Full journey, respawn/resize, default Learning, homographs, four Learning beacons, victory/restart, 496 image URLs, no page errors; release/deployment tracked in STATUS |

Workflow #1 and Gemini lane #12 were saved/pushed before gameplay fixes. Prototype
static check and refusal to overwrite an existing prototype pass. Approved walk
sheet processing produced an ignored preview without changing production assets.
The standard skill validator could not run because its Python environment lacks
PyYAML; that is not reported as a pass. The portable skill has required frontmatter
and works as a directly readable instruction file regardless of auto-discovery.

Meaning service: one live read-only cat lookup confirmed the existing worker's
elementary dictionary response. Not all 496 meanings/same-POS senses were reviewed;
missing/offline is explicit. Browser tests use mocks, not paid speech. The first
motion assertion initially used an unreliable 120ms sleep; it now waits for the
first observed position change and tests direction, avoiding a timing-only failure.

Known later work: #9 downloaded fixed narration, #10 first-line silence, #11
wardrobe. Do not close these on the strength of this release. Real-device sound
and teacher-level semantic review remain in docs/RELEASE-CHECK.md.

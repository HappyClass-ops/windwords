# First fix batch — 6 September 2026

Owner scope: #13 homograph acceptance (Sol), #10 first story audio (Sol),
#18 story dialogue clipping (Terra). No paid generation, push or deployment.

## Coordination

Live GitHub check found all three open, ready and unclaimed. Claims identify the
actual workers, branches and base 5dce57e283eb43a05140cb0ace7c845b0d420559.
Sol (gpt-5.6-sol, medium) works sequentially on #13 and #10 in codex/batch-sol.
Terra (gpt-5.6-terra, medium) handles #18 in codex/batch-terra.
Both received fresh ticket/area context and were prohibited from spawning agents.
The coordinator owns integration on codex/first-fix-batch.

Overlap boundary: Sol owns vocabulary/narration JavaScript, replay markup and
logic tests; Terra owns story CSS and a dedicated viewport test. Package test
registration and resume notes belong to the coordinator. Separate worktrees
prevent shared-checkout writes. No additional review agents are assigned.

## Acceptance evidence

Implementation and verification results will be recorded after integration.
Real-iPad listening is a separate manual check; mocked audio cannot prove it.

## Next recommended batch

#14 (Sol): checkpoint distractor correctness, after #13 acceptance evidence.
#6 (Astra): walkable routes and door transitions; already ready, independent of
vocabulary logic. Check shared game.js ownership before concurrent editing.
#17 (Terra) is the next small UI candidate, but overlaps #6 in world/shop files
and should follow that integration or use a carefully bounded isolated branch.
These are recommendations only, not claims or authorization to begin.

## Integration corrections

The coordinator caught an invalid negative test for noun trim. A haircut is a
common noun use and must be accepted alongside adjective/verb. Sol was asked to
correct both metadata and test before final verification. Source:
[Cambridge Dictionary: trim](https://dictionary.cambridge.org/dictionary/english/trim).

The coordinator requested geometry assertions for ordinary story pages one/two,
not only successful scroll-and-click, so the layout test detects the original
clipping rather than merely proving scroll fallback. Actual browser UI zoom must
remain distinguished from a CSS zoom approximation.

## First batch verified checkpoint

#13: Sol commits 2840cc4 / 1556cba, integrated as 67a18a3 / 7817bdb.
Metadata audit removed three non-playable keys (watch/sound/plant), added common
trim noun/verb, nest verb and sink noun uses, preserved all catalogue entries,
and aligned feedback with acceptance. This is a bounded metadata audit, not a
claim that every possible English sense was individually researched.

#10: Sol left code/tests when its usage limit was reached; primary coordinator
completed the takeover. Pre-edit fake trace was say then stop; fixed trace is
stop then say. Replay appears for unavailable/timeout. Fake lifecycle and actual
voice-module rejection tests cover Next, Skip, mute, back, rejected playback,
rejected browser fallback and stale completion. Real iPad remains unverified.

#18: Terra left CSS and viewport test; primary coordinator finished scrollable
reading text with fixed reachable controls, focusable reading area, 44px targets,
and 16px minimum text. Ordinary first/second pages, extra replay, long reading,
keyboard access, 1024x768, 768x1024, 390x844, 1024x480 and CSS zoom 200% pass.
Actual browser UI zoom remains unverified; CSS zoom is only an approximation.

First combined npm test: PASS all 13 test programs (the previous nine plus
vocabulary acceptance, story audio, story viewport and voice autoplay). No paid
speech/image calls. Owner subsequently authorized solo execution of the remaining
queue and push/deployment after verification. No further agents will be spawned.

## Solo selection and presentation slice

Actual worker: primary coordinator, no agents. #14 now partitions candidates by
shared accepted uses and samples finite unique pools: exact requested right count
when available, fewer options on shortage. #22 remembers the last two presented
rounds, prefers unseen candidates and relaxes oldest first; new run clears history,
help/panels do not. Normal/beacon/phase transitions share this policy.
#19 labels both result returns Back to village. #21 mirrors only the sprite child
before takeoff, retains direction on nearly vertical jumps. #32 replaces gear
announcements with truthful beacon/region text; fixed inventory recorded.
Verification: node tests/selection.cjs (all phases/kinds/adverb settings and small
banks), node tests/solo-fixes.cjs (both result paths, saved balance, all outfits,
left/right/vertical jumps, run-history reset, integrated checkpoint counts),
node tests/vocabulary-acceptance.cjs PASS. Final release suite still pending.

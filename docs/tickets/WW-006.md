# Replace ad hoc walking with walkable routes and visible door transitions

Triage snapshot: 2026-09-06. Current request is tickets/planning only, not permission to start coding.

- Report points: 2, 3, 8
- Category: Reopened movement bug / architecture
- Priority: P1
- Recommended AI: Astra (high reasoning)
- Deliverable: Implementation with a small navigation contract first
- Readiness: ready-for-agent
- Blocked by: None

## What happened / expected

Pip walks off the path, misses the book door, can disappear entering the shop and changes direction oddly toward its far-right item. Prior direct-line acceptance was insufficient.

Give each scene a small authored walkable route graph (or equivalently simple walkable polygons), real entrance/interaction anchors and explicit approach/enter/appear states. Use the shortest walkable route, not the shortest line through obstacles.

## Reproduction / evaluation scenario

From each village destination go to Word book, Map, shop and Go play; in the shop choose every ordered pair of counter items, including rapid retargeting and return.

## Acceptance criteria

- [ ] A source-image-to-screen transform aligns floor paths and feet anchors across responsive scene sizes; no cliff/roof/counter traversal.
- [ ] Arrive at actual doors; switch scene only after reaching the entrance, with an intentional transition and valid receiving spawn. No unexplained missing Pip.
- [ ] Routes are authored/testable; a small weighted graph is the default, not a physics engine or another forced down-across-up lane.
- [ ] Verify all destination pairs, cancellation, rapid clicks, resize/orientation, touch/reduced motion and shop enter/exit.
- [ ] Keep shop panel occlusion in its separate ticket; retain existing sprites and no free roaming.

## Routing and boundaries

Screen geometry, sprite feet, obstacles, retargeting and scene handover need one coherent treatment rather than another coordinate patch.

Another model may attempt this when the owner explicitly assigns it, but must announce the recommendation mismatch BEFORE edits and respect blockers, scope and permissions. Recommendation is not an exclusive lock. Check the repository's agent instructions and the world area guide; do not reread the whole conversation.

No paid generation, secret changes, curriculum expansion, production push or deployment is authorized by this planning pass. A later implementation assignment must retain its own cost and release boundaries.

## Evidence / scope note

Reopen #6 because new visual QA disproves the earlier route-completeness claim. The world module is about 15KB; complexity is the missing geometry/transition model, not an enormous pathfinding engine. Report 2 is shop/village movement, not necessarily the map modal.

User report and static inspection are not a completed reproduction test. Claim actual worker/branch/base in a comment, keep one active status label, and close only with acceptance evidence. GitHub is the live status; this file is a dated brief.

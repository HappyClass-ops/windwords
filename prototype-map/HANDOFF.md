# #24 map discovery prototype

Question: inspect discoveries/familiar words while keeping the map separate from
level selection. One panel, as requested in the ticket; not a wider variant study.

Run from this worktree: `node scripts/dev.cjs`, then open
http://127.0.0.1:4173/prototype-map/index.html.

Tap familiar, newly visited and locked examples. Mock data stays explicitly
labelled; locked state exposes no island identity or images; no persistence,
production imports, API calls or word-mastery claim. Existing images copied.
Primary source stays on codex/map-discovery-prototype, outside deployment.
Owner approval is still required before #25 promotes a production design.
